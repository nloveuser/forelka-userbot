# Forelka Userbot 2.0

🚀 **Modern, modular Telegram userbot with multi-account support and web interface**

## 📌 Описание

Forelka Userbot — это полностью переработанная версия популярного Telegram userbot'а с современной архитектурой, поддержкой нескольких аккаунтов и красивым веб-интерфейсом.

### ✨ Особенности

- **🏗️ Модульная архитектура** — легко добавлять и управлять функциями
- **👥 Мультиаккаунт** — поддержка нескольких Telegram аккаунтов
- **🌐 Веб-интерфейс** — красивый и удобный интерфейс управления
- **🔒 Система владельцев** — гибкое управление правами доступа
- **⚙️ Гибкая конфигурация** — JSON-конфигурация с настройками
- **📊 Инлайн-бот** — просмотр логов и статуса через Telegram
- **🔄 Автообновление** — проверка и установка обновлений
- **💾 Резервное копирование** — автоматическое создание бэкапов
- **📝 Расширенное логирование** — подробные логи с фильтрацией

## 🛠 Установка

### Требования

- Python 3.8+
- Git

### Быстрый старт

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/your-repo/forelka-userbot.git
   cd forelka-userbot
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте конфигурацию:**
   ```bash
   cp config.sample.json config.json
   # Отредактируйте config.json с вашими настройками
   ```

4. **Запустите бота:**
   ```bash
   # Без прав root
   python3 -m forelka
   
   # С правами root (с предупреждением)
   python3 -m forelka --root
   ```

## 🚀 Использование

### CLI Команды

```bash
# Добавить аккаунт
python3 -m forelka --add-account USER_ID API_ID API_HASH

# Удалить аккаунт
python3 -m forelka --remove-account USER_ID

# Просмотреть аккаунты
python3 -m forelka --list-accounts

# Запустить с кастомной конфигурацией
python3 -m forelka --config my_config.json
```

### Команды бота

- `.help` — помощь по командам
- `.ping` — проверка ответа
- `.prefix [новый_префикс]` — изменить префикс
- `.owner [user_id|reply]` — управлять владельцами
- `.alias [name] [target]` — создать алиас
- `.modules` — список модулей
- `.load [module]` — загрузить модуль
- `.unload [module]` — выгрузить модуль
- `.reload` — перезагрузить модули
- `.backup` — создать бэкап
- `.update` — проверить обновления

## 🌐 Веб-интерфейс

После запуска веб-интерфейс будет доступен по адресу: `http://127.0.0.1:8080`

### Функции веб-интерфейса:

- **📊 Dashboard** — общая информация и статус
- **👥 Accounts** — управление аккаунтами
- **📦 Modules** — загрузка и управление модулями
- **📝 Logs** — просмотр и фильтрация логов
- **⚙️ Settings** — настройка параметров

### Авторизация

По умолчанию:
- **Логин:** `admin`
- **Пароль:** `admin`

**⚠️ Обязательно измените пароль после первого входа!**

## 🏗️ Архитектура

```
forelka/
├── core/                    # Ядро системы
│   ├── bot.py              # Главный класс бота
│   ├── database.py         # Менеджер базы данных
│   ├── config.py           # Конфигурация
│   ├── module_loader.py    # Загрузчик модулей
│   ├── command_handler.py  # Обработчик команд
│   └── logger.py           # Логирование
├── modules/                # Встроенные модули
│   ├── help.py             # Система помощи
│   ├── ping.py             # Проверка ответа
│   ├── owner.py            # Управление владельцами
│   ├── prefix.py           # Управление префиксами
│   ├── alias.py            # Система алиасов
│   ├── ubinfo.py           # Информация о боте
│   ├── logs.py             # Просмотр логов
│   ├── load.py             # Загрузка модулей
│   ├── backupmanager.py    # Резервное копирование
│   └── Updater.py          # Обновления
├── web/                    # Веб-интерфейс
│   ├── app.py              # Flask приложение
│   ├── routes.py           # Маршруты
│   └── templates/          # HTML шаблоны
├── inline/                 # Инлайн-бот
│   └── bot.py              # Telegram инлайн-бот
└── utils/                  # Утилиты
    └── helpers.py          # Вспомогательные функции
```

## 📊 База данных

Система использует SQLite базу данных с следующими таблицами:

- **accounts** — информация об аккаунтах
- **owners** — владельцы аккаунтов
- **settings** — настройки
- **modules** — информация о модулях

## 🔧 Конфигурация

### Основные настройки

```json
{
    "database_path": "forelka.db",
    "log_level": "INFO",
    "web_interface": {
        "enabled": true,
        "host": "127.0.0.1",
        "port": 8080,
        "secret_key": "your-secret-key"
    },
    "inline_bot": {
        "enabled": true,
        "token": "your-bot-token",
        "owner_id": 123456789
    },
    "modules": {
        "auto_load": true,
        "modules_dir": "modules",
        "loaded_modules_dir": "loaded_modules"
    }
}
```

## 🚨 Безопасность

- **Root-пользователь** — система предупреждает о запуске от root
- **Владельцы аккаунтов** — только владельцы могут управлять аккаунтом
- **Веб-интерфейс** — требует аутентификации
- **Секретные ключи** — обязательно измените значения по умолчанию

## 🔄 Обновления

Система поддерживает автоматическое обновление через Git:

```bash
# Проверить обновления
.python3 -m forelka update

# Применить обновления
.python3 -m forelka update apply
```

## 📝 Разработка

### Создание модулей

1. Создайте файл в папке `modules/`
2. Добавьте метаданные:
   ```python
   __developer__ = "Your Name"
   __version__ = "1.0"
   __description__ = "Module description"
   ```

3. Реализуйте функции и регистрацию:
   ```python
   async def my_command(client, message, args):
       # Ваш код
   
   def register(bot, commands, module_name):
       commands.register_command("mycommand", my_command, module_name,
                               description="Описание команды",
                               usage=".mycommand [аргументы]")
   ```

### Система прав

- **owner_only** — только для владельцев аккаунта
- **admin_only** — только для администраторов
- **public** — доступно всем

## 🤝 Вклад в развитие

1. Форкните репозиторий
2. Создайте ветку с описательным названием
3. Внесите изменения
4. Создайте pull request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE.

## 📞 Поддержка

- **Канал:** [@forelkauserbots](https://t.me/forelkauserbots)
- **Поддержка:** [@forelusersupport](https://t.me/forelusersupport)
- **Issues:** [GitHub Issues](https://github.com/your-repo/forelka-userbot/issues)

---

**Forelka Userbot 2.0** — мощный, гибкий и безопасный инструмент для Telegram!
